{
  "name": "khushvel-v10-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Building V10 and Fintech Portfolio...' && (cd fintech-portfolio && CI=false npm install && CI=false npm run build)"
  }
}
